{
	"title": "SCF Date Picker Field Fragment",
	"description": "Type-specific properties for date picker fields",
	"type": "object",
	"properties": {
		"type": {
			"enum": [ "date_picker" ]
		},
		"display_format": {
			"type": "string",
			"default": "d/m/Y",
			"description": "Date format shown to user in field UI"
		},
		"return_format": {
			"type": "string",
			"default": "d/m/Y",
			"description": "Date format returned by get_field()"
		},
		"first_day": { "$ref": "#/definitions/first_day" },
		"default_to_current_date": { "$ref": "#/definitions/default_to_current_date" }
	}
}
